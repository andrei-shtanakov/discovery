# runtime-v1 — implementation run (maestro)

Открыт **на запуске**, до первого агента (spec §11 readiness). Живая приёмка —
отдельный прогон и отдельный файл доказательств (spec §12, TASK-016).

## Объявление прогона

| | |
|---|---|
| Режим (ADR-ECO-007) | `ecosystem-development` |
| `write_scope` | `{discovery}` — целевой репо не входит; его добавляет только прогон живой приёмки |
| Конфиг | `project.yaml`, Mode-2, `maestro orchestrate` |
| Базовая ветка | `pilot/runtime-v1` (master не трогается; человеческий гейт — финальный PR) |
| Воркстримы | `a1-contract` → (`b-state` ∥ `c-protocol`) → `d1-cli` |
| Задачи | TASK-001…013 из `spec/tasks.md` (13 из 16; A2 и D2 вне прогона) |
| `max_concurrent` | 2 |
| Модель | sonnet, зафиксирована в `spec_runner.claude_model` |

## Состояние на запуске

**Launch commit: `c9398edae8912d4244125cf580309c587decced9`** — HEAD `pilot/runtime-v1`
в момент открытия этого файла (мерж PR #8 в master). Коммит с этим файлом —
его прямой потомок и добавляет только его; бэклог, план и конфиг, по которым
работают агенты, — это дерево launch commit'а.

Один SHA вместо per-file хешей: бэклог, план и конфиг лежат вне scope всех
воркстримов, поэтому их правка агентом — запись вне scope, которую ловит
scope-гейт и фиксирует история `ws/*` вместе с диффом.

Инструменты: spec-runner 2.33.2, claude 2.1.235 (Claude Code), uv 0.11.23.

## Readiness (fail-closed, до первого агента)

- [x] `maestro validate project.yaml` — 0 ошибок (warning'и `scope-no-match` на
      ещё не созданные файлы; `--strict` не применяется, причина названа в конфиге)
- [x] identity репо: `git@github.com:andrei-shtanakov/discovery.git`, дерево чисто
- [x] `git config --local core.worktree` пуст
- [x] `workspace_base` (`discovery-maestro-ws`) свободен
- [x] число задач сверено: 16 в бэклоге, 13 в прогоне
- [x] launch commit записан выше

## Ход прогона

### Попытка 1 (07:10–07:23) — остановлена на A1, дефект инструмента

`a1-contract` трижды прошёл `DECOMPOSING → READY → RUNNING → FAILED` и исчерпал
ретраи → `NEEDS_REVIEW`. B/C/D1 не стартовали. Полезной работы не выполнено.

Причина: `spec-runner plan --full` сгенерировал мета-строку
`🔴 **P0** | Est: **2h** | **Depends on:** … `, которую отвергает валидатор
`spec-runner run` (ожидается `🔴 P0 | ⬜ TODO | Est: 2h` — приоритет, СТАТУС,
оценка). Промпт генератора требует правильную форму (`cli_plan.py:672`) —
отклонилась модель, и результат не проверялся перед передачей исполнителю.
Заведено: **spec-runner#301** (inbox).

Почему не чинится правкой файла: ручная правка даёт `0 errors, 0 warnings`, но
установленный maestro регенерирует спеку на каждом входе в воркстрим
(«Always regenerate» в `orchestrator.py`), так что правка стирается.

Что сделано вместо: требование к формату мета-строки вписано в описание всех
четырёх воркстримов в `project.yaml` — генератор читает его вместе с заданием.
Это единственный рычаг, целиком лежащий в нашем репо.

Две сопутствующие находки, записаны в spec-runner#301: ретрай на невалидном
входе перегенерирует вместо починки (три полные генерации на одинаковый отказ),
и быстрый выход оставляет пустой `logs/<ULID>/spec-runner-<pid>.jsonl` —
диагноз возможен только ручным воспроизведением `run --all --dry-run`.

### Попытка 2 (08:12–08:26) — формат исправлен, всплыли две другие причины

Мета-строка сгенерировалась канонически (`🔴 P0 | ⬜ TODO | Est: 2h`) — рычаг
через описание работает, spec-runner принял файл и взял TASK-001 в работу.
Но воркстрим упал дважды, и обе причины другие.

**Причина A — претензия на red по уже существующему файлу.** Настоящее
сообщение исполнителя (в логах прогона его нет, добыто ручным
воспроизведением `spec-runner run --all --spec-prefix maestro-`):

```
⛔ RED not confirmed, refusing to implement: no confirmed red for this task in
this workstream — the red was written into tests/test_task_001_red.py, which
already existed before this task started. A claim freezes the whole file, so
the implementation could not add to it afterwards
```

Файл остался от попытки 1: ветка `task/task-001` **пережила удаление
worktree**, потому что ветки живут в репо, а не в рабочей копии. Снос
`ws/a1-contract` их не трогает. Урок для чистки после неудачного воркстрима:
удалять и `task/*`-ветки, иначе следующая попытка получает HOOK_FAILURE без
ретрая.

**Причина B — в репо не было `.gitignore`.** Red-коммит `0c50e58` содержит
`__pycache__/test_task_001_red.cpython-313-pytest-9.1.1.pyc` и `uv.lock`, но не
исходник теста. `.pyc` попадает в коммит и участвует в претензии на red.
Заведён `.gitignore` (`__pycache__/`, `logs/`, `spec/.executor-*`, кеши).

**Причина C — моя инструкция протекла в требования.** Формат мета-строки,
вписанный в описание воркстрима, генератор превратил в `[REQ-012] Any
downstream-generated spec/maestro-tasks.md uses the mandatory meta-line
format… Traces to TASK-001, TASK-002` — то есть в требование к
разрабатываемому рантайму, который `tasks.md` не пишет вообще (граница
author ≠ execute). Инструкция переоформлена как явная мета-инструкция
генератору с запретом заводить её как REQ/NFR/TASK.

Отдельная находка о наблюдаемости: причина A **не видна из прогона** —
`logs/<ULID>/spec-runner-<pid>.jsonl` пуст, таблицы `attempts`/`agent_calls`
состояния исполнителя пусты, в БД maestro только «spec-runner exited with
code 1». Диагноз потребовал ручного воспроизведения в worktree.

### Попытка 3 (08:29–08:35) — прошла дальше всех, вскрыла дефект плана

Две прошлые причины не воспроизвелись: мета-строка каноническая, red-фаза честная —
коммит `e400393` с настоящим тестом на 42 строки, без `.pyc`. Тест прогнан вручную:
падает по assertion об отсутствующем поведении, а не по ImportError, то есть red
законный.

**Причина D — дефект плана, а не инструмента.** `Task 1 Step 3` предписывал
`uv run tools/vendor_pull.py ../discovery-toolkit`. Из worktree
`discovery-maestro-ws/a1-contract/` путь `../discovery-toolkit` не существует: рядом
лежит только сам worktree, а upstream-чекаут — в `~/labs/all_ai_orchestrators/`.
Шаг писался из основного чекаута, где сосед действительно рядом. TASK-001 был
невыполним в принципе.

Что именно этот шаг оборвал конкретный прогон — не доказано (лог фазы реализации снова
пуст), но недостижимость upstream доказана и достаточна.

**Исправление: пред-вендоринг.** Копия контракта и линтера положена на базовую ветку
руками, байты взяты `git show <commit>:<path>` из **дерева коммита**, а не из рабочей
копии соседа — иначе провенанс не гарантирован. Обе сверки совпали. TASK-001 теперь
пишет `tools/vendor_pull.py` и тестирует его **герметично** против поддельного upstream
в `tmp_path`; ни одна задача больше ничего не требует снаружи своего worktree. Тот же
приём заранее чинит TASK-014 (банк), который уткнулся бы в тот же путь.

### Попытка 4 (08:59–09:01) — регресс на уже пройденную стадию

Упала **на red**, хотя попытка 3 red проходила. Причина:

```
uv run pytest → error: Failed to spawn: `pytest` — No such file or directory
```

`pyproject.toml` был голым скелетом (`dependencies = []`, dev-зависимостей нет):
`pytest` в проекте не установлен. Red-коммит `d95c8ec` содержит только тест и
`uv.lock`, без `pyproject.toml`. В попытке 3 тот же агент **сначала** правил
`pyproject.toml` (red-коммит `e400393` включал 5 строк) и потому шёл дальше.

**Разница между попытками была не в наших правках, а в порядке действий агента.**
Всё это время мы наблюдали лотерею: структурная причина — TDD требует красный тест
первым, а первая задача плана несла настройку тулчейна, без которой тестов не
бывает, — присутствовала во всех четырёх прогонах.

**Правило, которого не хватало и которое теперь записано:** базовая ветка
TDD-прогона обязана быть рабочей средой до старта — входы завендорены, тулчейн
установлен, `test_command` зелёный. Всё, что этого требует, не может быть задачей
для агента.

### Обновление maestro (перед попыткой 5)

Установленный maestro был 0.4.0 при том же `version = "0.4.0"` в репо — но **на 167
коммитов позади** master: номер версии не двигали, поэтому совпадение номеров ничего
не доказывало (проверка: маркер `_CONFIG_OPTION_HELP` — 15 вхождений в исходниках, 0
в установленном). Значимое из потерянного: per-project состояние
(`state_paths.py`, PR #186, ломающее) и `#166 resume без regen` — ровно то поведение,
из-за которого правка сгенерированной спеки была бесполезна, а каждый ретрай стоил
полной генерации.

Обновлено установкой из remote на `master@ffb5fa5` (не из рабочего дерева соседа: оно
грязное, и правки уехали бы в бинарь). Проверено: `state_paths.py` на месте, `--config`
появился. Прогон больше не передаёт `--db` — состояние ложится под
`~/.maestro/projects/<host>/<owner>/<repo>/runs/<run-id>/`.

Ни один из пяти отказов установленной версией вызван не был; но общая БД и
перегенерация на ретрае сделали разбор дороже, чем он должен был быть.

### Попытка 5 (09:09–09:16) — дошла дальше всех, причина видна впервые

База была проверена в чистом worktree (`pytest` стартует, `ruff check` чист, линтер
импортируется), но упала на 148-й секунде. **Обновлённый maestro впервые дал разбор
сам** — `runs/<id>/postmortem/<ws>/<ts>/` с манифестом, снимком состояния исполнителя и
логами. Прошлые четыре попытки требовали ручного воспроизведения; здесь причина
прочиталась из `attempts`:

```
RED not confirmed: lint failed on the file about to be frozen
(tests/test_task_001_red.py) — 2 files would be reformatted.
After a checkpoint it is byte-immutable, so this must be fixed before the red is fixed.
```

**Причина E — `per-file-ignores` не действует на форматтер.** В базе я закрыл
вендоренный `gate_check.py` через `[tool.ruff.lint.per-file-ignores]`, но это правило
линтера; `ruff format` про него не знает и хотел переписать файл. Команда линта —
`check && format --check`, поэтому вендоренные байты были защищены наполовину.
Исправлено на `[tool.ruff] extend-exclude`, который действует и на линтер, и на
форматтер.

Гейт здесь сработал правильно и поймал ровно то, ради чего заведён: попытку
переформатировать файл, чья побайтовая неизменность и есть предмет доказательства.

**Причина F — план содержал код, нарушающий формат-правила проекта.** `ruff format`
разбирает python-блоки внутри markdown, и красным оказался сам план: сниппеты
`vendor_pull.py` и герметичного теста. Агент, копирующий их дословно (а план это прямо
предписывает), получал бы красный линт. Сниппеты приведены к формату, `lambda`-
присваивание заменено на `def` (E731).

Заодно: `logs/` оказались в индексе git (мой `git add -A` до появления `.gitignore`) —
артефакты прогонов лежали в репозитории. Убраны из индекса.

### Попытка 6 (09:29–09:52) — РАБОТА ВЫПОЛНЕНА, остановлена гейтами

**Обе задачи A1 закрыты**: 676 строк в 13 файлах, восемь коммитов с честным
TDD-циклом (red → реализация → code-review fixes → мерж task-веток).
`exit_code: 0`, `done: 2 / planned: 2`. Проверено независимо: 18 тестов,
ruff чист, и главное — **диф вендоренных файлов пуст**: агент работал вокруг
`gate_check.py` и не изменил в нём ни байта. Ради этого две гарантии и заводились.

**Остановка 1 — ex-post scope-гейт.** Вне объявленного scope оказались четыре
файла: два red-артефакта TDD (создаются на каждую задачу), `conftest.py` и
`tests/test_vendor_pull.py` — последний мой недосмотр: добавил тест в план при
починке пред-вендоринга, а в `project.yaml` дописать забыл. Гейт прав по всем
четырём. Scope дополнен во всех воркстримах (red-файлы — непересекающимися
диапазонами номеров задач).

**Ревью Codex-критика: approval не дан, четыре содержательных дефекта.**

- **P0.** `drift` необратимо самоблокировался: у первого прогона нет предыдущего
  успешного запуска → `unknown` → job красный → успешный запуск не появится
  никогда. Дефект целиком мой и поучительный: `last_success` я ввёл в ответ на
  замечание Copilot про неиспользуемый аргумент — починка одного породила худшее.
  Свежесть вахты нельзя доказывать изнутри проверяемого workflow.
- **P0.** `pyrefly` красный (3 ошибки) на вендоренном файле, который нельзя
  править. Нужен был `project-excludes`, а не `search-path`.
- **P1.** Не было governance-caller'а, обязательного для первого PR с кодом
  (`CLAUDE.md`). Скопирован побайтово из флота (`a0243b1d67c4`).
- **P1.** `vendor_pull --include-frames` искал банк по несуществующему
  `upstream/frames/`. Герметичный тест был зелёным **потому, что подделывал
  upstream в неверной форме** — проверял согласие инструмента с самим собой.
  Введено отображение source→dest + регресс на форму путей.
- Плюс: манифест `PINNED.txt` сам стал предметом проверки (`EXPECTED_SURFACE`) —
  удаление строки тихо выводило файл из-под обеих гарантий, оставляя их зелёными.

После починки: **19 passed, ruff чист, pyrefly 0**.

**Остановка 2 — гейт completeness.** Resume заново прошёл `DECOMPOSING`, а
состояние исполнителя помнит обе задачи закрытыми, поэтому в текущем исполнении
выполнено «0 из 2». Рассинхрон бухгалтерии, вызванный тем, что четыре дефекта я
чинил руками: коммиты `e7ebf54` оркестратору неизвестны.

**Открытый вопрос владельцу:** одобрить completeness, чтобы прогон дошёл до
финализации и ex-post scope-гейт наконец отработал после починки. Важно не
приписать себе лишнего: **scope-гейт в заходе 06:26 не запускался** — прогон
остановился раньше, поэтому доказательства, что починка scope сработала, пока нет.

### Попытка 6 — линт зелёный целиком

Запущена с изолированной БД `~/.maestro/discovery.db`: первая попытка шла в
общую `~/.maestro/maestro.db` и очистила 7 воркстримов от чужого прогона от
08.08 (строки `tasks` целы). Установленный maestro 0.4.0 держит одну БД на все
проекты; per-project раскладка есть только в свежих исходниках.

## Сверка после прогона

Заполняется после финального PR: статусы TASK-001…013 в `spec/tasks.md`,
расхождения сгенерированных `spec/maestro-*` с планом, записи вне scope.


## Прогон 01M0CCHWKN (07:00–07:21) — чистый, снова остановлен scope-гейтом

Работа выполнена целиком (`2/2 done`), остановка только на гейте, и escape
сократился с пяти путей до двух: `conftest.py` и `tests/test_vendor_pull.py`
объявлены верно, а **глоб с символьным классом не сработал**.

**Причина G — матчер scope не поддерживает символьные классы.** В
`maestro/scope_gate.py::_glob_to_regex` прямо написано: `**` — любое число
сегментов, `*` — внутри сегмента, `?` — один не-слэш, «every other character is
literal». Значит `tests/test_task_00[12]_red.py` сравнивался буквально и не мог
совпасть ни с чем. Валидация молчала: на момент `maestro validate` этих файлов
ещё нет, и предупреждение `scope-no-match` неотличимо от «файл будет создан».

Исправлено перечислением red-файлов поштучно во всех четырёх воркстримах.

### Предполётная проверка scope — новый обязательный шаг readiness

Два прогона подряд потеряны на декларации, которую проверял только сам прогон.
Теперь она проверяется до запуска — матчером **самого maestro**, его же
интерпретатором (не копией семантики у нас):

```sh
~/.local/share/uv/tools/maestro/bin/python - <<'EOF'
from maestro.scope_gate import find_escapes, normalize
import yaml
cfg = yaml.safe_load(open("project.yaml", encoding="utf-8"))
for w in cfg["workstreams"]:
    print(w["id"], find_escapes(normalize(EXPECTED_PATHS[w["id"]]), w["scope"]))
EOF
```

Для A1 ожидаемый набор берётся фактом из evidence-ветки прошлого прогона
(`git diff --name-only $(git merge-base pilot/runtime-v1 evidence/…)..evidence/…`),
для остальных — прогнозом по плану. Результат: 14 / 12 / 15 / 5 путей, escape
нет ни в одном воркстриме.

Ловушка внутри самой проверки, на которую я наступил с первого раза: диф от
текущего `pilot` включает коммиты, легшие туда ПОСЛЕ ветвления, и даёт ложный
escape. Считать только от `merge-base`. Проверка, которая врёт, хуже отсутствующей.
